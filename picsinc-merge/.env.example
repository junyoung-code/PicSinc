# Server-only values. Do not use a NEXT_PUBLIC_ prefix for secrets.
SUPABASE_URL=https://zogtpmolcmpiipwbysck.supabase.co
SUPABASE_SECRET_KEY=
SUPABASE_STORAGE_BUCKET=picsinc-merge

# Optional local Python override; defaults to ../experiments/yolo-outline/.venv/bin/python
# YOLO_PYTHON=/absolute/path/to/python
# Packaged runtime directory in Docker; optional override for local development
# YOLO_RUNTIME_DIR=/app/yolo

# Vercel server only: independently generated random values (at least 32 characters).
UPLOAD_SIGNING_SECRET=
WORKER_TOKEN=
