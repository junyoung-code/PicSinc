# Copy to .env.worker. No Supabase key is needed on the worker.
WORKER_BASE_URL=https://your-project.vercel.app
WORKER_TOKEN=
# Optional overrides; defaults point to the existing local YOLO installation.
# YOLO_PYTHON=/absolute/path/to/experiments/yolo-outline/.venv/bin/python
# YOLO_RUNTIME_DIR=/absolute/path/to/experiments/yolo-outline
# Windows: set YOLO_DEVICE=cuda:0 and Windows absolute paths for YOLO_PYTHON and YOLO_RUNTIME_DIR.
# Windows setup and quality gates: deploy/windows-worker.md
